node_modules
dist-ssr
*.local
.vite
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local data — kept out of git so personal info never lands in commit history.
# These live on the spare's disk and are backed up separately.
server/data.json
server/comments.json
server/staff.json
server/staff-board.json
server/programs.json
server/rules.json

# Local environment overrides
.env
.env.local
.env.*.local
