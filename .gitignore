node_modules
dist-ssr
*.local
.vite
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Note: Data files (data.json, programs.json, etc.) are now committed to git
# so they're available when accessing the app from any device via craniaverse.ngrok.app

# Local environment overrides
.env
.env.local
.env.*.local
